# データのメンテナンス手順

価格やモデルの情報は、すべて `data.js` の 1 ファイルに集約しています。
ここを編集して `index.html` をブラウザで再読み込みするだけで、サイトに反映されます。
HTML / CSS / app.js を触る必要はありません。

---

## 1. data.js の構造

```js
window.MAC_DATA = {
  updated: "2026年6月時点（2026/6/25 価格改定を反映）", // 画面右上と注記に出る更新日
  source: "...",                                        // 補足メモ（画面には影響なし）
  models: [
    {
      id: "mba13",                 // 内部用の一意なID（重複不可）
      family: "MacBook Air",       // シリーズ名（カードや比較表に表示）
      name: "MacBook Air 13インチ", // 機種名（カード見出し）
      tagline: "驚くほど軽い。...",  // キャッチコピー
      type: "ノートブック",          // "ノートブック" か "デスクトップ"
      accent: "#b7c9e2",           // カードのアイコン色（任意の色コード）
      // legacy: true,             // 前世代モデルのときだけ付ける（省略=現行）
      // year: "2025年モデル",      // 前世代のときの世代ラベル
      colors: ["スカイブルー", "シルバー"], // カラー名（表示のみ・価格に影響なし）
      chips: [
        {
          id: "m5-8c",        // 機種内で一意なチップID
          name: "M5",         // チップ名
          cpu: "10コアCPU",    // 先頭の数字がCPUコア数として差分計算に使われる
          gpu: "8コアGPU",     // 先頭の数字がGPUコア数として差分計算に使われる
          basePrice: 184800,  // この構成の開始価格（円）
          memory: [
            { label: "16GB", delta: 0 },     // 先頭は必ず delta: 0（標準）
            { label: "24GB", delta: 30000 }, // 標準からの追加額（円）
            { label: "32GB", delta: 60000 },
          ],
          storage: [
            { label: "512GB", delta: 0 },    // 先頭は必ず delta: 0
            { label: "1TB", delta: 30000 },
            { label: "2TB", delta: 90000 },
          ],
        },
      ],
    },
    // 他のモデルが続く
  ],
};
```

価格の計算式は次のとおりです。

```
表示価格 = chips の basePrice + 選択中メモリの delta + 選択中ストレージの delta
```

---

## 2. よくある更新作業

### A. 価格が改定されたとき（最も多い作業）

該当モデルの `basePrice` を新しい開始価格に書き換えます。
アップグレード額が変わった場合は `memory` / `storage` の `delta` を直します。

```js
basePrice: 184800,   // 例: 199800 に書き換え
```

最後に、画面の更新日表示も直しておくと分かりやすいです。

```js
updated: "2026年6月時点（...）",  // 新しい日付に書き換え
```

### B. 構成の選択肢を増やす / 減らす

`memory` か `storage` の配列に行を足す / 消すだけです。
delta は必ず昇順（小さい容量ほど安く、先頭は 0）にします。

```js
storage: [
  { label: "512GB", delta: 0 },
  { label: "1TB", delta: 30000 },
  { label: "2TB", delta: 90000 },
  { label: "4TB", delta: 210000 },  // この行を追加
],
```

### C. 同じ機種に新しいチップを追加する（例: 上位構成）

`chips` 配列にオブジェクトを 1 つ足します。

```js
chips: [
  { id: "m5", name: "M5", cpu: "10コアCPU", gpu: "10コアGPU", basePrice: 279800,
    memory: [ ... ], storage: [ ... ] },
  // 追加
  { id: "m5pro", name: "M5 Pro", cpu: "14コアCPU", gpu: "20コアGPU", basePrice: 369800,
    memory: [ ... ], storage: [ ... ] },
],
```

### D. 新しい機種・新世代モデルを追加する

`models` 配列に既存のモデルをコピーして貼り付け、中身を書き換えるのが簡単です。
`id` は必ず他と重複しない値にします。

新しい世代が出て、今までの現行モデルが「前世代」になった場合は、
古いほうのモデルに次の 2 行を足すと、世代間比較の対象になり「前モデル」バッジが付きます。

```js
legacy: true,
year: "2026年モデル",
```

逆に、新しい現行モデルは `legacy` / `year` を付けません（省略 = 現行）。

### E. モデルを削除する

`models` 配列から該当モデルのオブジェクト（`{ ... },`）を丸ごと消します。
直前・直後のカンマの数が合っているか確認してください。

---

## 3. 編集後の確認

1. 構文ミスがないかチェック（任意・Node がある場合）

   ```sh
   node --check data.js
   ```

   何も出なければ OK です。`SyntaxError` が出たら、カンマや波カッコ `{ }`・
   角カッコ `[ ]` の対応を見直してください。

2. `index.html` をブラウザで開く（すでに開いていれば再読み込み）。

   ```sh
   open index.html
   ```

3. 変更したモデルを選び、価格や差分が想定どおりかを目視で確認します。

---

## 4. つまずきやすいポイント

- 各行・各オブジェクトの末尾のカンマの付け忘れ / 付けすぎ（最も多い構文エラー）。
- `memory` / `storage` の先頭は必ず `delta: 0`（標準構成）にする。
- `delta` は基準からの「追加額」であって、合計額ではない。
- `cpu` / `gpu` は先頭の数字を差分計算に使うので、`"16コアCPU"` のように
  数字を先頭に置く（`"CPU 16コア"` のような順だとコア数の差分が出ません）。
- `id` はモデル間・チップ間で重複させない。
